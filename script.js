/**
 * this function takes info from the form and makes the query url
 * @returns {string}
 */
function buildQueryURl() {
    var qyeryURL = "https://api.nytimes.com/svc/search/v2/articlesearch.json?";
}